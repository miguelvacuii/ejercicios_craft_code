namespace Modulo1.Ejercicio1
{
    public class Product
    {
        public const string IVA = "16";
        // El uso como constante sería correcto si solo hay un valor,
        // Si hay más de un valor de IVA debería estar en una tabla en base de datos
        // Si no va a usarse desde fuera de la clase debería declararse como private
    }
}